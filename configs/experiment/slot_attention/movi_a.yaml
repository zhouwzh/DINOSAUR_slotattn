# @package _global_
defaults:
  - /experiment/slot_attention/_base_large
  - /dataset: movi_a_image
  - /experiment/slot_attention/_preprocessing_movi
  - /experiment/slot_attention/_metrics_clevr
  - _self_

trainer:
  devices: 4 #8
  max_steps: 1500000
  max_epochs:
  gradient_clip_val: 1.0

dataset:
  num_workers: 4
  batch_size: 8

models:
  conditioning:
    _target_: routed.ocl.conditioning.RandomConditioning
    object_dim: 256
    n_slots: 11
    batch_size_path: input.batch_size

load_checkpoint: /scratch/wz3008/new_SlotAttn/object-centric-learning-framework/outputs/slot_attention/movi_a/2026-02-02_05-55-06/lightning_logs/version_0/checkpoints/epoch=34-step=381993.ckpt